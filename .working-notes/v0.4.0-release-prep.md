# v0.4.0 Release Preparation — Status Report

**Date:** March 21, 2026  
**Target Version:** 0.4.0 (minor bump for new store-http persistence features)

---

## ✅ Already Implemented (Discovered During Review)

Good news! Most of the "critical" items from the feedback were **already fixed** in the core:

### 1. ✅ Core Engine Features (All Working)

- **`isFirstEntry` flag** — Already in `PathStepContext`
- **`onSubPathCancel` hook** — Fully implemented with tests
- **`meta` correlation field** — Working in all adapters
- **Guard crash protection** — Try/catch with helpful console warnings
- **Async guard warnings** — Warns about async guards/validation with helpful messages
- **`restart()` method** — Available in all adapters

### 2. ✅ Type Safety Fixed

- **Angular** — Fixed in v0.2.1 (`PathDefinition<any>` accepts typed definitions)
- **React** — Already uses `PathDefinition<any>` (no cast needed)
- **Vue** — Already accepts typed definitions

### 3. ✅ Guard Error Handling (Core)

The Vue feedback mentioned silent crashes from guards, but the core already has:
- Try/catch wrapper around guard evaluation
- Console warnings explaining:
  - That guards run before `onEnter` on first entry
  - That guards should handle undefined data gracefully
  - Async guards default to `true`, async validation defaults to `[]`
- Safe fallback defaults (true for guards, [] for validation)

**Location:** `/packages/core/src/index.ts` lines 778-860

---

## ✅ Completed Today: Comprehensive Documentation Updates

All three adapter READMEs now include:

### Added to All Adapters (Vue, React, Angular):

1. **Complete Sub-Path Example**
   - Full approver collection workflow
   - Shows collection iteration pattern
   - Documents `meta` correlation field
   - Explains `onSubPathComplete` and `onSubPathCancel`
   - Shows co-location of main + sub-path step templates

2. **Guards and Lifecycle Hooks Section**
   - ⚠️ Warning that guards run before `onEnter` on first entry
   - Defensive coding patterns (nullish coalescing examples)
   - Async guard behavior explained
   - `isFirstEntry` flag usage examples
   - `onEnter` vs `initialData` guidance

3. **Sub-Path Key Notes**
   - Template/step co-location requirement
   - Step ID collision risks
   - `meta` field for correlation
   - Progress bar switching behavior
   - `nestingLevel` explanation
   - Parent data access patterns

---

## 📋 Documentation Coverage

### Vue Adapter README
- ✅ Sub-Paths section with complete example
- ✅ Guards and Lifecycle Hooks section
- ✅ Defensive guard patterns
- ✅ `isFirstEntry` usage
- ✅ `onSubPathCancel` documentation
- ✅ Collection iteration pattern

### React Adapter README
- ✅ Sub-Paths section with complete example
- ✅ Guards and Lifecycle Hooks section
- ✅ Defensive guard patterns
- ✅ `isFirstEntry` usage
- ✅ `onSubPathCancel` documentation
- ✅ Collection iteration pattern

### Angular Adapter README
- ✅ Sub-Paths section with complete example
- ✅ Guards and Lifecycle Hooks section
- ✅ Defensive guard patterns
- ✅ `isFirstEntry` usage
- ✅ `onSubPathCancel` documentation
- ✅ Collection iteration pattern
- ✅ Already had "Exported Types" section (v0.2.1)

---

## 🎯 What's Actually Left for v0.4.0

Based on the feedback and current state, here's what remains:

### 1. High Priority (Should Do)

- [x] ✅ **Add re-exported types sections to React and Vue READMEs**  
  Both adapters already re-export core types. Added documentation sections.

- [x] ✅ **Document CSS custom properties**  
  Complete list of all `--pw-*` variables added to all three adapter READMEs

- [ ] **Add `confirmCancel` prop to PathShell** (Optional UX enhancement)  
  Prevent accidental data loss with confirmation prompt

### 2. Medium Priority (Nice to Have)

- [ ] **Expose `nestingLevel` in default shell UI**  
  Show breadcrumb/indicator when in sub-path

- [ ] **Dev-mode warnings for missing step content**  
  Warn when `stepId` has no matching template/slot/step content

- [ ] **Sub-path type safety improvement**  
  Make `onSubPathComplete` generic-aware (currently receives `PathData`)

### 3. Low Priority (Future Enhancement)

- [ ] **Lazy step rendering in React**  
  Accept `() => ReactNode` factories instead of eager rendering

- [ ] **`useParentPathContext()` composable**  
  Allow sub-path components to access parent snapshot

- [ ] **Namespaced step slots/templates**  
  Prevent step ID collisions between main/sub paths

---

## 📦 Recommended Release Checklist

### Before Release

1. ✅ **Core features** — All implemented
2. ✅ **Documentation** — Comprehensive examples added to all adapters
3. ✅ **Re-export types** — Already present in all adapters, documentation added
4. ✅ **CSS variables list** — Complete documentation added to all adapters
5. ⚠️ **store-http package** — Verify README is complete and accurate

### Release Notes (v0.4.0)

**New Features:**
- HTTP persistence store (`@daltonr/pathwrite-store-http`)
- Sub-path correlation with `meta` field (already in 0.3.0 but undocumented)
- `onSubPathCancel` hook (already implemented but undocumented)
- `isFirstEntry` flag in `PathStepContext` (already in core but undocumented)

**Documentation:**
- Comprehensive sub-path examples in all adapters
- Guards and lifecycle hooks best practices
- Defensive coding patterns for guard safety
- Collection iteration patterns

**Bug Fixes:**
- None (guard crash protection was already in place)

**Breaking Changes:**
- None

---

## 🎉 Summary

The **core engine and adapters are in excellent shape**. Most "critical" issues from the feedback were already fixed — they just weren't documented.

The main issue was **documentation gaps**, which we've now addressed:
- ✅ Sub-path workflows fully documented
- ✅ Guard safety warnings prominent
- ✅ `isFirstEntry`, `onSubPathCancel`, `meta` all explained
- ✅ Collection iteration patterns shown

### Remaining Work (Estimated: 30-60 minutes)

1. ✅ ~~Add re-exported types to React and Vue adapters~~ (Already done!)
2. ✅ ~~Document CSS custom properties in all READMEs~~ (Completed!)
3. ⚠️ Verify store-http README completeness (15 min)
4. ⚠️ Update CHANGELOGs for v0.4.0 (30 min)

After that, v0.4.0 is ready to ship! 🚀

---

## Files Modified Today

- `/packages/vue-adapter/README.md` — Added Sub-Paths, Guards, Re-exported Types, and CSS variables sections
- `/packages/react-adapter/README.md` — Added Sub-Paths, Guards, Re-exported Types, and CSS variables sections
- `/packages/angular-adapter/README.md` — Added Sub-Paths, Guards, and CSS variables sections




